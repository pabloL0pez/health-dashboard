declare const styles: {
  readonly filterWidget: string;
  readonly filtersContainer: string;
  readonly selected: string;
  readonly filterOptions: string;
  readonly resetAllButton: string;
}

export default styles;